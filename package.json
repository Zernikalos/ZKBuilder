{
  "name": "zkbuilder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:node": "lerna run build:node",
    "build:node:builder": "lerna run build:node --scope=@zernikalos/zkbuilder",
    "build:node:cli": "lerna run build:node --scope=@zernikalos/zkbuilder-cli",
    "build:browser": "lerna run build:browser --scope=@zernikalos/zkbuilder",
    "build": "lerna run build",
    "generate": "lerna run generate --scope=@zernikalos/zkbuilder",
    "clean": "lerna run clean",
    "dev": "lerna run start --parallel",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "newversion": "lerna version --no-push --yes",
    "publish": "lerna publish"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Zernikalos/ZKBuilder.git"
  },
  "devDependencies": {
    "@zernikalos/zernikalos": "next",
    "lerna": "9.0.1"
  },
  "private": true,
  "workspaces": [
    "packages/zkbuilder",
    "packages/zkbuilder-cli"
  ]
}
